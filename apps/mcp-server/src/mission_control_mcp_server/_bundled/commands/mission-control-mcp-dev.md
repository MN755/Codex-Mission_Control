---
description: Develop or diagnose MCP/plugin integration through Mission Control
disable-model-invocation: false
---

Ask Mission Control to handle MCP or plugin development for:

`$ARGUMENTS`

Require:

- Tool/resource/prompt contract review.
- Auth and token boundary check.
- Stdio startup validation.
- Host reload guidance.
- Cross-platform command check.
- Regression tests where possible.

Keep the standalone dashboard out of scope unless the user explicitly asks for UI work.

---
description: Design retrieval, indexing, RAG, and context-pack workflows
disable-model-invocation: false
---

Ask Mission Control to design a retrieval or RAG workflow for:

`$ARGUMENTS`

Require corpus scope, chunking, metadata, ranking, freshness, privacy posture, and eval cases. Prefer local-first retrieval unless the user explicitly approves external embedding or vector services.

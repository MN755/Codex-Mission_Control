# Approval Relay

Mission Control has raised a pending decision.

Render it cleanly for the user:

- Decision type
- Why it is needed
- Risk level
- Exact command or action if available
- Safer alternatives if they exist

Then ask the user for an explicit answer and send that answer back through the Mission Control decision tool.

Pending decision payload:

```json
{{DECISION_PAYLOAD}}
```

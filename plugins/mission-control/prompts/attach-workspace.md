# Attach Workspace

Attach the current folder to Mission Control.

Rules:

1. Decide whether this looks like a new project or an existing codebase.
2. If it is an existing codebase, use a read-only scan first.
3. Do not assume Mission Control created the repo.
4. Do not require API keys for Codex login.
5. Report the attached project identifier or link once the workspace is registered.

Context:

```text
Workspace path: {{WORKSPACE_PATH}}
User request: {{USER_REQUEST}}
```

# Existing Codebase Fix Bug

User:

`Attach Mission Control to this workspace and fix the failing tests in the backend.`

Expected bridge behavior:

1. Attach the existing repo.
2. Request the read-only codebase scan first.
3. Show the understanding summary.
4. Pass the failing-test request to the Mission Control Manager.
5. Relay approvals or questions back to the user.
